apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chatbot-wiki-pvc
  namespace: chatbot
  labels:
    app: chatbot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # Optional: Specify storage class
  # storageClassName: standard
